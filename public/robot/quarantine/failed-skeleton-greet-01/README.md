# Failed Skeleton Greet 01

This folder contains converted GLB outputs from `ac36077d-05d7-4e15-95de-1a4576f358dd_fbx_greet_01.fbx`.

The original FBX remains untouched in `public/robot/original_tripo_exports/` for audit. The converted outputs are quarantined because visual inspection showed a failed Johnny export: the skeleton/mesh did not line up correctly, producing a mismatched gray character that does not belong in the runtime viewer.

Do not include these files in randomization, behavior mapping, or runtime imports.

Quarantined files:

- `greet_01.glb`
- `greet_01_optimized.glb`
